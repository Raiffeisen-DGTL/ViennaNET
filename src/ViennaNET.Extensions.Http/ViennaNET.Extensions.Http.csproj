<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net6.0</TargetFramework>
        <OutputType>Library</OutputType>
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
    </PropertyGroup>

    <PropertyGroup>
        <Description>
            Предоставляет средства типовой конфигурации HTTP клиентов.
            Поддерживает политики Retry и Timeout политики, по умолчанию,
            а так же, возможность добавлять собственные политики или переопределять существующие.
            Поддерживает функцию, расширенного ведения журнала, позволяя записывать в журнал
            тело, запроса или ответа, на уровне Trace, не закрывая чувствительные данные.
            Предоставляет API для настройки способа аутентификации.
        </Description>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.Extensions.Configuration.Abstractions" Version="2.2.0" />
        <PackageReference Include="Microsoft.Extensions.Http.Polly" Version="6.0.11" />
    </ItemGroup>

</Project>
