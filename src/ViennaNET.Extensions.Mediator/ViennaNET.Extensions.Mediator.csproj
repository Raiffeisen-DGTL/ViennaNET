<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net6.0</TargetFramework>
    </PropertyGroup>

    <PropertyGroup>
        <Description>
            Предоставляет API на основе реализации шаблона пректирования "Посредник".
            Используйте Mediator для реализации обмена информацией между объектами, формируя при этом слабую связанность, 
            путём избавления объектов от необходимости явно ссылаться друг на друга.
            Доступно 3 способа обмена информацией:
                1. отправка сообщения одному получателю-обработчику;
                2. отправка сообщения-запроса одному получателю-обработчику (ожадается ответ);
                3. отправка сообщения-события нескольким получателям-обработчикам.
        </Description>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Debug' ">
        <DocumentationFile>bin\Debug\ViennaNET.Extensions.Mediator.xml</DocumentationFile>
    </PropertyGroup>

    <PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
        <DocumentationFile>bin\Release\ViennaNET.Extensions.Mediator.xml</DocumentationFile>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="6.0.0" />
    </ItemGroup>

</Project>
